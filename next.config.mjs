/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["fresh-strapi.onrender.com", "api.freshestudiocreativo.com"],
  },
};

export default nextConfig;
